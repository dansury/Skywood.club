# 2026-05-16 — Drop Node, flatten to repo root

The site is now PHP-only and deployed straight from the repository root.

## Removed

- Node.js backend: `src/`, `package.json`, `package-lock.json`.

## Changed

- Moved the contents of `public/` to the repository root (`index.html`,
  `order.html`, `install.php`, `.htaccess`, `.env`, `css/`, `js/`, `assets/`,
  `api/`, `data/`). The repo root is now the deployable web root.
- `pull-config.php` `subdir` set to empty (pull the whole repo root).
- `.gitignore`, `CLAUDE.md`, `README.md`, `spec.md`, `spec/*.md` updated for
  the PHP-only, root-level layout. `README.md` trimmed to deployment and
  credential steps only.
